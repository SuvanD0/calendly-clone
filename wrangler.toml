name = "calendly-clone"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[env.production]
name = "calendly-clone"

[[d1_databases]]
binding = "DB"
database_name = "calendly-db"
database_id = "" # Will be set after creating the database

[vars]
APP_URL = "" # Set in production via environment variables

